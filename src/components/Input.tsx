"use client";
import { Input as RAC_Input } from "react-aria-components";

function Input() {
  return (
    <div slot="inputWrapper" className="w-full bg-">
      <RAC_Input />
    </div>
  );
}

export default Input;
