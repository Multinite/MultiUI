{
  "title": "MultiUI Config Schema",
  "description": "Schema for defining a new MultiUI Config.",
  "type": "object",
  "properties": {
    "components_output_dir": {
      "type": "string",
      "description": "The output directory for components."
    },
    "framework": {
      "enum": ["react", "vue", "svelte", "ts"],
      "description": "The framework to use for components."
    },
    "package_manager": {
      "type": "string",
      "description": "The package manager to use for components."
    },
    "theme_prefix": {
      "type": "string",
      "description": "The prefix to use for theme class names."
    }
  }
}
